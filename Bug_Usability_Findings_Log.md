# Bug & Usability Findings Log (Kịch Bản D)

Dưới đây là bảng tổng hợp tất cả các lỗi giao diện (Bug) và vấn đề trải nghiệm người dùng (Usability) phát hiện được trong quá trình kiểm thử phân hệ **Yêu cầu hỗ trợ (Support Requests)**.


| ID Lỗi | Kịch bản / Màn hình | Loại | Mô tả lỗi / Trải nghiệm thực tế | Các bước tái hiện / Heuristic vi phạm | Mức độ nghiêm trọng (0-4) | Đề xuất sửa đổi (Fix Recommendation) | Tham chiếu ảnh (Filename) | Thời điểm gửi Form |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **BUG-01** | (D3) Admin - Bộ lọc danh sách | **Bug** | Các ô nhập "Tìm tên...", "Mã thành viên", và "Loại yêu cầu" không có nhãn (label) phía trên, trong khi hai ô nhập ngày "Từ ngày" và "Đến ngày" lại có nhãn phía trên. Điều này dẫn đến việc các ô nhập không được canh lề thẳng hàng theo phương ngang (các ô chọn ngày bị đẩy lệch xuống dưới), tạo cảm giác giao diện lệch hàng, không chuyên nghiệp. | 1. Đăng nhập tài khoản Admin.<br>2. Đi tới trang quản lý yêu cầu hỗ trợ (D3).<br>3. Quan sát bộ lọc tìm kiếm. | **1 (Minor)** | *GUI-04: Canh lề và khoảng cách* (Shneiderman #1: Nhất quán).<br>Đồng nhất cách hiển thị bằng cách thêm label cho toàn bộ các ô nhập hoặc bỏ label ngoài và đưa vào placeholder, đảm bảo các ô nhập được canh lề giữa (vertically centered) thẳng hàng trên cùng một dòng. | `BUG_D3_Filter_Misalignment.png` | 03/08/2026 18:47 |
| **BUG-02** | (D3) Admin - Danh sách yêu cầu | **Bug** | Dropdown "Số dòng mỗi trang" ở thanh phân trang dưới bảng bị đơ; khi click chọn các giá trị khác (10, 50, 100) hệ thống không thay đổi số lượng dòng hiển thị mà vẫn giữ nguyên ở mức mặc định là 20. | 1. Vào danh sách yêu cầu Admin (D3).<br>2. Click dropdown "Số dòng mỗi trang" ở góc dưới.<br>3. Chọn một giá trị bất kỳ (10, 50, hoặc 100). | **2 (Moderate)** | *GUI-32: Pagination hoạt động đúng* (Nielsen #1: Phản hồi trạng thái hệ thống).<br>Liên kết sự kiện click của các item trong dropdown với hàm gọi lại API có tham số `pageSize` tương ứng để cập nhật giao diện. | `BUG_D3_Pagination_Stuck.png` | 03/08/2026 18:48 |
| **BUG-03** | (D2) User - Chi tiết yêu cầu | **Bug** | Icon lịch/đồng hồ hiển thị thời gian gửi bị lệch hàng, không được căn giữa dọc (vertically aligned) một cách đồng đều so với khối văn bản 2 dòng bên cạnh ("Thời gian gửi" và thông tin ngày giờ). | 1. Vào trang chi tiết yêu cầu hỗ trợ của User (D2).<br>2. Quan sát phần thời gian gửi ở góc trên bên phải của card thông tin yêu cầu hỗ trợ. | **1 (Minor)** | *GUI-04: Canh lề và khoảng cách* (Nielsen #8: Thiết kế tối giản và thẩm mỹ).<br>Sử dụng CSS flexbox (`display: flex; align-items: center;`) cho thẻ div bao quanh icon và khối văn bản để căn chỉnh icon đều theo chiều dọc. | `BUG_D2_Icon_Misalignment.png` | 03/08/2026 18:50 |

